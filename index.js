// 1. Function expression for division
const divide = function() {
    return 2000 / 100;
  };
  
  // 2. Arrow function for squaring a number
  const square = x => x * x;
  
  // 3. Arrow function for addition
  const add = (a, b) => a + b;
  